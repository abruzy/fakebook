<% flash.each do |key, value| %>
	<% key = "success" if key == "success" %>
	<% key = "danger" if key == "danger" %>
  <div class="alert alert-<%= key %>">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <strong><%= value %></strong>
  </div>        
<% end %>