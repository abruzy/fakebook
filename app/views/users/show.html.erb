<div class="card start-feed">
  <div class="card-body start-feed-content">
    <div>
      <a <i class="fa fa-pencil"> <%= link_to "start a post", new_post_path %> </i></a>
    </div>
    <div>
      <a <i class="fa fa-camera"> <%= link_to "upload an image", new_post_path %> </i> </a>
    </div>
    <div>
      <a href="#"> <i class="fa fa-newspaper-o"> </i></a>
    </div>
  </div>
</div>
<% if !@posts.blank? %>
<% @posts.each do |post| %>
<div class="card mt-4">
  <div class="card-header">
    <div class="card-flex">
      <div><%= gravatar_for post.user , 40 %></div>
      <div class="post-header">
        <p class="post-header-detail"><%= link_to post_owner(post), post.user%></p>
        <p class="post-header-detail"><%= post.user.email%></p>
        <p class="post-header-detail"><%= time_ago_in_words(post.created_at) %> ago</p>
      </div>
    </div>
  </div>
  <div class="card-body">
    <p class="card-text"><%= link_to truncate(post.description), post %></p>
    <% if post.image %>
    <img class="card-img-top" src="..." alt="Card image cap">
    <%end%>
  </div>
  <div class="card-footer text-muted">
   <a> <i class="fa fa-thumbs-up">Like</i> </a>
   <a><i class="fa fa-comments-o"> Comment</i> </a>
  </div>
</div>
<%end%>
<% else %>
Nothing Here
<%end%>